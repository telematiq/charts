apiVersion: v1
kind: Service
metadata:
  name: {{ include "tnext-backend.fullname" . }}
  labels:
    {{- include "tnext-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tnext-backend.selectorLabels" . | nindent 4 }}
