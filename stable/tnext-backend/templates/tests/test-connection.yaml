apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tnext-backend.fullname" . }}-test-connection"
  labels:
    {{- include "tnext-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tnext-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
